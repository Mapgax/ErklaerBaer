# Documentation archive

Current instructions live in [Recovery and next steps](../RECOVERY_AND_NEXT_STEPS.md).

- `OPERATIONS_pre_v3.md`: original daily operating model, superseded by the weekly v3 direction.
- `2026-09-09-before-recovery-consolidation/`: 22 exact snapshots taken before the recovery-guide consolidation. `index.json` maps original project paths to snapshots and SHA-256 hashes. Older setup/export blockers and permissions in these snapshots are historical.

To recover one previous document, find its `original` entry in the dated index, verify the snapshot hash and copy it to a separate comparison location first. Do not replace current instructions with stale history unless intentionally reverting a documented change. Relative links inside snapshots retain their original text and resolve correctly after restoration to the original project path.

Media, assets and earlier code ZIPs were retained in their existing versioned folders, not moved here. This archive is local and is not an independent disaster-recovery backup.
